 # Netlify config to use the official Next.js plugin for full Next.js support
 [build]
   command = "npm run build"
   publish = ".next"

 [[plugins]]
   package = "@netlify/plugin-nextjs"

 [context.production.environment]
   NODE_ENV = "production"
